# Package Source

Planned home for package-level TreeK source files.

Current working files still live in `pkg_treek`:

- `pkg_treek/pkg_treek.xml`
- `pkg_treek/treek_install_script.php`
- `pkg_treek/language/`
- `pkg_treek/packages/`

This directory should remain documentation-only until a build step or explicit
path migration is introduced.

