# Package-уровень TreeK

Здесь лежат файлы верхнего уровня Joomla package.

Сборщик читает отсюда:

- `pkg_treek.xml`
- `treek_install_script.php`
- `language/`

Вложенные ZIP не хранятся здесь. Они собираются из соседних каталогов `src`:

- `src/plugin-ajax-treek/`
- `src/file-treek-kunena/`
